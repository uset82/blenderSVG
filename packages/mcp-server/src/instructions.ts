export const MCP_SERVER_INSTRUCTIONS = [
  "Studio keeps work on local frames. Read the current frame before changing it.",
  "Use get_styles before choosing colors or type, and stay inside the project style.",
  "After an edit, call screenshot_frame and check the image before the next change.",
  "One undo step reverts the last applied canvas edit."
].join(" ");
