/**
 * Phase 1 workspace boundary only. PixiJS is introduced in Phase 7 after the
 * shared protocol, state machine, extension shell, Webview, and SVG fallback
 * have met their acceptance criteria.
 */
export const runtimePixiPackageId = "@codex-avatar-studio/runtime-pixi";
